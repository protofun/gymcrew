import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="This is a placeholder — profile & settings aren't built yet."
    />
  );
}
