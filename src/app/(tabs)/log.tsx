import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function LogScreen() {
  return (
    <PlaceholderScreen
      title="Log Workout"
      description="This is a placeholder — quick workout logging isn't built yet."
    />
  );
}
