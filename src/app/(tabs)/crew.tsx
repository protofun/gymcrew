import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function CrewScreen() {
  return (
    <PlaceholderScreen
      title="Crew"
      description="This is a placeholder — the crew feed isn't built yet."
    />
  );
}
