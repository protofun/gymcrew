import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function RanksScreen() {
  return (
    <PlaceholderScreen
      title="Ranks"
      description="This is a placeholder — the rank leaderboards aren't built yet."
    />
  );
}
