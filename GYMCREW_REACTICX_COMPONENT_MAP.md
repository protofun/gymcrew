# GymCrew ↔ Reacticx Component Map

Generated by the Phase 0 discovery audit. This is a **planning map**, not a completed migration record — per AGENTS.md §37, nothing here should be read as "already migrated." Update the "Status" column as each component actually moves (Phase 3 onward).

## Real Reacticx catalog — corrected 2026-09-16 via `npx reacticx list` (the CLI registry, not the docs website)

**Correction to this document's earlier draft**: the first pass of this catalog was built from `reacticx.com`'s indexed docs pages via WebFetch, which turned out to disagree with the actual CLI registry in real ways (missing whole categories, different names, wrong claim that no chart component exists). Phase 1 ran `npx reacticx init` and `npx reacticx list` directly against the live registry — **that CLI output is authoritative** for what `reacticx add` can actually install, and is what this document now reflects. Local project config: `component.config.json` → `{ outDir: "src/components/ui", packageManager: "npm" }`.

**163 components** across 11 categories (counts from the live registry):

| Category | Count | Notable items |
|---|---|---|
| atoms | 1 | `pressable` |
| base | 15 | `button`, `animated-input-bar`, `otp-input`, `curved-bottom-tabs`, `flip-card`, `marquee`, `qr-code`, `ruler`, `scrollable-search`, `radiant-button`, `squircle-view`, `gradient-avatar`, `border-beam`, `fade-component`, `shimmer-wave-text` |
| **charts** | **5** | **`bar-chart`, `line-chart`, `pie-chart`, `radar-chart`, `radial-chart`** — corrects the earlier claim that Reacticx has no charting component |
| blocks | 19 | Full pre-built sections: `profile-settings-v1..v4`, `welcome-v1..v4`, `signup-v1`, `billing-v1`, `email-verification-v1`, `empty-inbox-v1`, `empty-gallary-v1`, `empty-collection-v1`, `empty-gift-v1`, `add-address-new`, `airbnb-v1`/`air-bnb-v2`, `apple-v1` |
| micro-interactions | 10 | `elastic-slider`, `flexi-button`, `gooey-switch`, `hamburger`, `spin-button`, `stacked-chips`, `animated-theme-toggle`, `expandable-view`, `save-button`, `verified-shine` |
| molecules | 45 | Largest category: `accordion`, `bouncy-accordion`, `disclosure-group`, `context-menu`, `fan-menu`, `gooey-popover`, `search-bar`, `Pagination`, `Shimmer`, `Toast`, `circular-list`, `range-slider`, `seek-bar`, `split-view`, plus ~9 carousel variants (`blur-`, `cinematic-`, `circular-`, `material-`, `parallax-`, `rotate-`, `scale-`, `tilt-`, `vertical-flow-`/`vertical-page-carousel`) and several text-effect components (`dynamic-text`, `gooey-text`, `dust-text`, `gradient-wave-text`, `letter-swarm`) |
| organisms | 40 | `check-box`, `progress`, `circular-progress`, `segmented-control`, `theme-switch`, `animated-header-scrollview`, `infinite-menu`, `matched-geometry`, `masked-tab-bar`, `mobile-dock`, `tray`, `unfold-menu`, plus a large FX/orb set (`apple-intelligence`, `aurora`, `chroma-ring`, `energy-orb`, `grainy-gradient`, `liquid-metal`, `mesh-gradient`, `nebula-orb`, `radial-intro`, `skia-ripple`, `wave-scrawler`, `shockwave`, `metal`, `siri-ios-27`) and more text-effects (`animated-text`, `circular-text`, `curved-marquee`, `fade-text`, `liquid-chrome-text`, `staggered-text`, `dia-text`) |
| pieces | 10 | `profile-card`, `photo-stack`, `polaroid`, `coupon`, `event-ticket-card`, `receipt-card`, `barcode-badge`, `book-page`, `social-button`, `verified-badge` |
| **primitives** | **9** | `alert`, `avatar`, `dialog`, `icon-tile`, `list`, `ripple-button`, `switch`, `tabs`, `toggle` |
| screens | 1 | `v1` |
| templates | 8 | `chat-v1`, `settings-v1`, `sign-up-v1`/`v2`, `floating-sheet`, `media-list`, `parallax-header`, `property-detail-v1` |

**Corrected gap analysis vs. what GymCrew needs**:
- ✅ **Charts exist** (`charts/bar-chart`, `line-chart`, `pie-chart`, `radar-chart`, `radial-chart`) — the earlier draft's claim that no chart component exists was wrong. These are real migration candidates for `StrengthProgressChart`/`MetricTrendChart`/`ExerciseComparisonChart`/`WeeklyCalorieBars`, to be evaluated in Phase 3 for feature parity (min/max range filters, dual-series comparison, area fill) against `react-native-gifted-charts`, which GymCrew currently uses.
- ❌ **No dedicated bottom-sheet/drawer component in the real registry** — this is the opposite of what the docs-site pass suggested (which listed `bottom-sheet`/`bottom-sheet-stack`). The closest primitives are `dialog` (primitives) and the `floating-sheet` template (a full scaffold, not a composable low-level component). **This changes the plan for `BottomSheet.tsx` (11+ direct dependents, the single highest-risk component in this codebase): it most likely stays on `@gorhom/bottom-sheet` under the hood, re-skinned with Reacticx's motion/pressable primitives around it, rather than being swapped for a Reacticx sheet component that doesn't exist.** Re-verify with `reacticx info dialog` and `reacticx info floating-sheet` at the start of Phase 3 before committing to this.
- Still no generic `card` component, though `pieces/profile-card` and several `blocks/*` sections may cover specific card shapes.
- `primitives/list` exists and is worth evaluating for GymCrew's various row-list screens (crew roster, food log, notifications) in Phase 3.
- Text input: `base/animated-input-bar` and `base/otp-input` remain the closest fits; no plain multi-line labeled TextField primitive.
- Still no calendar or camera component — `react-native-calendars` (under `DatePickerModal`/`NutritionCalendar`) and `expo-camera` (under `PhotoCaptureGuide`/`scan-barcode.tsx`) stay as-is, re-skinned only.
- Some registry names are capitalized inconsistently (`Toast`, `Pagination`, `Shimmer` vs. lowercase-hyphenated everywhere else) — confirm exact `add` slugs work as listed before scripting any bulk install in Phase 3.

---

## Status legend
`not started` · `mapped (planning only)` · `in progress` · `migrated` · `verified on device (NOT VERIFIED yet for any row — no migration work has begun)`

## High fan-out / high-risk components (migrate first, most carefully — per AGENTS.md §22)

| GymCrew component | Fan-out | Reacticx target | Customization needed | Status |
|---|---|---|---|---|
| `BottomSheet.tsx` (wraps `@gorhom/bottom-sheet`) | 11 direct dependents, ~15+ sheets indirectly | `bottom-sheet` / `bottom-sheet-stack` | Must replicate documented web-platform workaround (forced remount via `key` to fix a gorhom dismiss/re-present bug) or confirm Reacticx's version doesn't have the same issue | not started |
| `EditableText.tsx` | 32 usages | No Reacticx equivalent (dev-tooling) | Keep as a thin wrapper around whatever Text primitive Reacticx screens use; preserve the Dev-Mode override behavior functionally | not started |
| `RankBadge.tsx` | 26 usages | No Reacticx equivalent (branded) | Re-skin the surrounding pressable/shadow chrome only; medal images untouched | not started |
| `ProgressBar.tsx` | 14 usages | `organisms/progress` (`AnimatedProgressBar`) | **Migrated** — kept the same `{ratio, color, height}` public API, thin wrapper maps onto the primitive's `progress`/`progressColor`/`animationDuration` (700ms, matches the original exactly) | **migrated** |
| `MuscleHeatmap.tsx` | 14 usages | No Reacticx equivalent (bespoke branded SVG) | Not portable to a generic component — wrap in updated chrome/typography only | not started |
| `SkewedStat.tsx` | 14 usages | No Reacticx equivalent (brand typography) | Keep as GymCrew-specific primitive | not started |
| `DivisionBadge.tsx` | 11 usages | No Reacticx equivalent (branded) | Re-skin chrome only | not started |
| `OnboardingFooter.tsx` | 16 usages | `base/button` (kept `OnboardingDots` custom, no `pagination` swap attempted) | **Migrated (Phase 4).** `isLoading` now crossfades label↔spinner via `Button.Content`/`Button.Loading`. Required adding a `fullWidth` prop to the vendored `Button` (it defaults to `alignSelf:"flex-start"` intrinsic sizing with no built-in way to stretch, which every GymCrew button needs). Visually verified via headless-browser screenshots — see Phase 4 progress entry. | **migrated** |

## Reacticx CLI quirks discovered while adding real components (Phase 3)

- **`reacticx add <name>` aborts the *entire* add silently-ish if any shared dependency file already exists**, even when that file is byte-identical and the actual new component hasn't been written yet (e.g. `reacticx add avatar` printed "1 file already exists, skipped" for `create-compound-component` and wrote nothing else at all — the `avatar/` directory itself never appeared). Fix: pass `-o`/`--overwrite` on every `add` after the first one that establishes a shared dependency. Always verify with `find src/components/ui/<category>/<name>` that the component's own directory actually landed, not just check the CLI's exit code.
- **`primitives/avatar`'s vendored source has a broken import path**: `avatar/index.tsx` imports `buildMesh` from `@/shared/components/base/gradient-avatar/utils`, but the CLI actually writes that dependency to `@/components/ui/base/gradient-avatar/utils` (matching this project's configured `outDir`). This is a real bug in the CLI's import-rewriting, not a config issue on GymCrew's side — fixed by hand in the vendored file after adding.
- **Several vendored components fail this project's lint config out of the box** (`react/display-name` errors on components wrapped in `memo()` without an explicit `.displayName`) — found in `Shimmer.tsx` (3 components) and `gradient-avatar/index.tsx` (1). Fixed by adding `.displayName = "..."` assignments; this is now expected boilerplate to check for on every new `add`. A handful of `react-hooks/exhaustive-deps` warnings remain in vendor animation code (`progress`, `avatar`, `gradient-avatar`) — left alone since they're warnings (not errors, don't fail `lint`) and touching Reanimated dependency arrays without deeper testing risks subtly changing animation restart behavior.
- **Passing `-e`/`-t` to copy examples/types is not worth it long-term**: the copied example screens (`src/shared/examples/<name>`) reference imports (`@/components`, `~/showcase`) that only exist in Reacticx's own demo app template, breaking `tsc --noEmit` immediately. Useful to fetch once for API research, then delete (`rm -rf src/shared/examples/<name>`) rather than leave in the tree. The copied `-t` types files (`src/shared/types/<name>`) are just a duplicate of the same types already exported from the component's own `types.ts` — skip `-t` entirely going forward and import types directly from the component.

## button

| GymCrew component | Reacticx target | Notes | Status |
|---|---|---|---|
| `SocialAuthButton.tsx` | `base/button` | **Migrated (Phase 4).** Same `fullWidth` addition as `OnboardingFooter` (see below). Visually verified — Google/Facebook rows render full-width with correct icons, no layout regression. | **migrated** |
| `UnitToggle.tsx` | none — stays custom | Investigated `organisms/segmented-control`: it defaults to full-row width ("screen width minus 32") and shows both options side by side — `UnitToggle` is a compact single-button tap-to-cycle control living inline as a `SliderField`/`Stepper` `rightAdornment` (5 call sites); the primitive doesn't fit that tight slot. | **kept custom** |
| `OnboardingFooter.tsx` | `base/button` + `OnboardingDots` | See high fan-out table above | **migrated** |

## card

**Built (Phase 5): `src/components/Card.tsx`** — no generic Reacticx card primitive exists (confirmed, see catalog correction above), so this is a GymCrew-composed shell, not a vendored component. Built on Reacticx's `atoms/pressable` (a real press-scale-animation Pressable, `expo-haptics`-capable) when `onPress` is given, plain `View` otherwise. Two variants (`default`/`accent`) and two corner radii (`large`/`medium`) cover every real card shell found in the app — grounded the same way Phase 2's tokens were: `default`+`large` (`rounded-3xl border-divider bg-surface`) was hand-duplicated in 4 components, `accent`+`medium` and plain `medium` were each their own one-off variant.

**Migrated onto `Card` (Phase 5, Home screen): `CrewCard`, `HomeNutritionWidget`, `LastWorkoutWidget`, `MuscleSuggestions`, `BiggestOpportunityCard`, `CrewWarWidget`** — all 6 kept their exact original `className` content, just split between `Card`'s fixed shell classes and each caller's own layout classes (margin/padding/flex/gap). Verified: `tsc`/`lint`/`expo export` clean; `Card`'s own press/variant/layout behavior visually confirmed via a temporary, uncommitted preview route (screenshotted, then deleted) since Home itself is auth+sync-gated and couldn't be screenshotted directly without real credentials. The 6 widgets' own visuals were **not** independently screenshotted — confidence there rests on the mechanical, diff-verified nature of the change (moving existing classNames, not rewriting them) plus `Card` itself being confirmed working. **NOT VERIFIED** on an authenticated run or a native simulator.

Still to compose under `Card` when their owning screens are migrated: `AnnouncementBanner` (deliberately left alone — distinct banner semantics, different border-color convention, not pressable as a whole), `ChallengeCard`, `GoalsWidget`, `MacroCard`, `NewPrsBanner`, `NutritionBanner`, `PeerDuelsCard`, `PrShareCard`, `RankRevealCard`, `SnapshotBanner`, `StatTile`, `WaterTracker`, `WelcomeWidget` (its "Start Workout" CTA was evaluated for a `Button.Root` swap and deliberately reverted — see button section below), `WorkoutShareCard`, `WorkoutStartCard`, `WorkoutTemplateCard` (its "hold to charge" long-press animation needs a custom Reacticx-composed build — port the existing Reanimated logic, don't rebuild from scratch).

**`WelcomeWidget`'s "Start Workout" CTA — attempted and reverted.** It needs `flex:1` behavior inside a `flex-row` (sitting next to a separate chevron icon), which `Button.Root`'s `fullWidth` (an `alignSelf` toggle, not `flexGrow`) doesn't directly provide — solvable by wrapping in a `flex-1` View, but the result couldn't be visually confirmed (Home is auth-gated) and this is the single most prominent CTA in the app. Reverted to the original plain `Pressable` rather than ship an unverified layout on the highest-visibility button in the app; revisit once real device/authenticated-session testing is available.

## input

| GymCrew component | Reacticx target | Notes | Status |
|---|---|---|---|
| `FormField.tsx` | none — stays custom | Investigated `base/animated-input-bar`: it's a completely different UX pattern (a search/chat-prompt input with a rotating, character-by-character-animated placeholder), not a labeled form field — GymCrew's auth/onboarding forms need a persistent label and a focus-border highlight, not cycling placeholder text. No other registry component (checked `otp-input`, `search-bar`) fits a "label above input" field either. Kept as-is per AGENTS.md §35 (don't force a mismatched component). | **kept custom** |
| `Stepper.tsx` | none — stays custom | **There is no numeric-stepper component in the real registry at all** (AGENTS.md's brief assumed one exists). `micro-interactions/spin-button` looked like the closest name match but is actually a submit-button-with-loading-spinner, unrelated. `molecules/range-slider`/`seek-bar` are drag-based continuous sliders, a different interaction model than discrete +/- taps with tap-to-type. Kept as-is. | **kept custom** |
| `Slider.tsx` / `SliderField.tsx` | `elastic-slider` or `squiggly-slider` | Evaluate feel vs. current `@react-native-community/slider` | not started |
| `SearchableSelectField.tsx` | `search-bar` + `dropdown`/`picker` | Composition | not started |
| `CreateExerciseForm.tsx`, `CreateFoodForm.tsx`, `ExerciseSetRow.tsx` | composition of above | Extract embedded calc logic (see Screen/Feature inventories) before re-skinning | not started |

## sheet/modal
**Decided: `BottomSheet.tsx` stays on `@gorhom/bottom-sheet`.** Investigated `templates/floating-sheet` as the one remaining candidate (its file list — `onDrag`/`onDragEnd`/`onMinimize`/`onSizeChange`/`present`/`dismiss` — looked promising for real gesture-driven sheet behavior). Adding it revealed it's actually a **media-player demo screen** (references `@/assets/video/sza.mp4`) built on a *different* third-party library, `@lodev09/react-native-true-sheet` (plus `expo-video`), not a Reacticx-native primitive at all. Introducing a second bottom-sheet library alongside the already-working, already-fixed `@gorhom/bottom-sheet` would violate AGENTS.md §11 ("do not introduce a second unrelated UI library... unless unavoidable and explicitly justified") with zero justification — `@gorhom/bottom-sheet` already provides real drag-to-dismiss, dynamic sizing, and keyboard avoidance, and GymCrew's `BottomSheet.tsx` already has a hard-won, empirically-documented fix for its one real web bug. **Reacticx genuinely has no bottom-sheet primitive to migrate to.** `BottomSheet.tsx` is therefore out of scope for a swap — it can still be re-skinned (colors/radius already pull from `@/theme`) but its underlying gesture/sizing engine stays as-is. `primitives/dialog` remains the right fit for simple non-draggable modal dialogs.

**`ConfirmModal.tsx` — migrated.** Rebuilt on `Dialog.Root`/`Portal`/`Overlay`/`Content`/`Header` in controlled mode (`open={visible}`), with GymCrew's own `colors.neutral.surface`/`divider` overriding the primitive's generic dark palette via `Content`'s `style` prop. Both buttons stay plain `Pressable`s rather than `Dialog.Close` — `Dialog.Close`'s `asChild` clone unconditionally overwrites the child's `onPress` with its own close handler, which would silently drop `onConfirm`. The 4 call sites (`crew/settings.tsx`, `profile/admin-challenges.tsx`, `profile/account.tsx`, `workout/active.tsx`) needed no changes — same public API.

**`VerificationCodeModal.tsx` — migrated (Phase 4).** Rebuilt on `base/otp-input`, a real upgrade: built-in shake-on-error, auto-clear-after-1s-then-refocus, and multi-digit paste/OS-autofill splitting, none of which the old hand-rolled boxes had. One accepted visual difference — only the currently-focused box highlights (vs. the old "every typed box stays yellow," not overridable via props); judged a common-enough OTP pattern on its own not to be worth forking the vendored component over. `ConfirmModal` was already migrated in the first Phase 3 batch (see high-risk table above).

Once `BottomSheet` is settled, the rest follow: `AddGoalModal`, `AdminChallengeFormSheet`, `AvatarActionSheet`, `CreateChallengeModal`, `CrewActivitySheet`, `DatePickerModal` (keep `react-native-calendars` underneath, just re-skin), `DayWorkoutsSheet`, `DuelChallengeSheet`, `ExerciseActionsSheet`, `ExerciseInstructionsModal`, `ExercisePickerModal`, `FilterPickerSheet`, `FoodPickerModal` (consolidate with `ExercisePickerModal`'s structure — they duplicate each other), `GoalDetailModal`, `InviteMembersModal`, `MemberActionsSheet`, `ReportModal`, `ShareCardModal` (keep `react-native-view-shot` underneath), `TierPickerSheet`, `TimePickerModal`, `TodayWorkoutModal`, `WorkoutOptionsSheet`, `WorkoutSettingsModal`, `WorkoutSummaryModal`, `XpProgressModal`, `NotificationsDropdown` → `dropdown`.

## chart
**Investigated and declined for now: `charts/line-chart` vs. `StrengthProgressChart`.** Added and read the primitive's full compound API (`Root`/`Line`/`Area`/`Grid`/`Indicator`/`Cursor`/`Tooltip`). It's a real, Skia-driven chart engine with pan-gesture support (`enablePan`, `onPointChange`) — but it has **no axis-label rendering at all**: `data` points are raw `{x, y}` numbers (not date strings), and there's no Y-axis tick-label or X-axis label component anywhere in its API, only `Grid` (plain horizontal reference lines, no text) and `Tooltip` (touch-only, not a persistent axis). GymCrew's `StrengthProgressChart` (8 dependents across workouts/nutrition/crew) relies heavily on `react-native-gifted-charts`' built-in formatted X-axis date labels and Y-axis numeric ticks — reproducing that on top of `line-chart` would mean building a full custom axis-label layer from scratch, i.e. more work than what `gifted-charts` already gives for free with brand-matched styling. **Decision: keep `react-native-gifted-charts` under `StrengthProgressChart`/`MetricTrendChart`/`ExerciseComparisonChart`, do not swap.** The same axis-label gap likely extends to `charts/bar-chart` (same author/design, not independently verified) — re-check specifically if `WeeklyCalorieBars`/`HorizontalBarChart` are ever revisited, since those have simpler axis needs (a 7-bar week, no date-range scrubbing) where the gap might matter less.

`ProgressBar` → `organisms/progress` — **migrated**, see the high fan-out table above. `GoalRing` — no direct equivalent found; GymCrew's current `react-native-svg` circular-progress implementation is simple enough that swapping isn't worth it. `BmiGauge`, `SegmentedBar`, `SegmentedProportionBar`, `MacroTotalsBar` — no dedicated primitives either; simple enough to rebuild natively with Reacticx layout primitives if touched, but not urgent.

## badge/rank-visual
Almost entirely **"preserve as-is, re-skin chrome only"** — see Screen Inventory §"GymCrew-branded visuals." No Reacticx mapping applies to `MuscleHeatmap`, `MuscleGroupPicker`, `RankBadge`, `DivisionBadge`, `DivisionAvatarFrame`, `CrewIconBadge`, `BadgeRevealFx`, `DivisionUpOverlay`, `TierGradientBackground`, `MuscleRankRow`, `VisualTrainingCalendar`, `IconBadge` (→ could adopt `badge` for the shell, keep custom color logic). `DivisionCelebrationWatcher` is a store-watcher, not a visual component — no change needed beyond wherever it renders `DivisionUpOverlay`.

## list-item
Simple compositions, no dedicated Reacticx primitive needed: `AchievementRow`, `ContributorsList`, `CrewFeedList`, `FoodLogRow`, `MealItemRow`.

## header
`AuthHeader`, `OnboardingHeader`, `TopBar` — no Reacticx header primitive; keep custom, re-skin only. `LegalScreenLayout` (bundles `LegalScreenLayout` + `LegalSection`) — trivial, no change needed.

## nav
`TabBar` (custom bottom tab bar w/ raised FAB) → consider `curved-bottom-tabs` or `morphing-tab-bar` as the base, keeping the raised-FAB composition (`TabBarFab`) as a GymCrew customization on top. `NutritionNavBar` follows the same pattern once `TabBar` is settled.

## toast/skeleton/loading

| GymCrew component | Reacticx target | Notes | Status |
|---|---|---|---|
| `AppToast.tsx` | `molecules/Toast` | Confirm Reacticx's toast can host the 4 branded variants (success/error/info/achievement) or keep `react-native-toast-message` underneath | not started |
| `Skeleton.tsx` / `HomeSkeleton.tsx` | `molecules/Shimmer` | **Migrated.** Kept the exact `{width, height, radius}` public API (dropped the unused `className` prop, never actually passed at any call site). Switched from the old flat opacity-pulse to the primitive's `"shimmer"` sweep variant — its `"pulse"` variant renders as a tinted overlay on a separate base color rather than one block changing opacity, so it doesn't reduce cleanly to the old look; the sweep uses GymCrew's own `divider`/`surfaceElevated` tones instead of the primitive's default gray. `HomeSkeleton.tsx` needed no changes — same block sizes/layout, only the loading motion changed. | **migrated** |
| `ErrorBoundary.tsx` | n/a | Must remain a class component (React requirement); only its fallback UI can be re-skinned | not started |

## avatar

| GymCrew component | Reacticx target | Notes | Status |
|---|---|---|---|
| `AvatarStack.tsx` | `primitives/avatar` | **Migrated.** No `avatar-group` exists in the real registry (see catalog correction), so the overlap/stack layout stays hand-rolled exactly as before; each avatar image is now `Avatar.Root`/`Avatar.Image` instead of a raw `Image`, which is a real (small) upgrade — the primitive tracks load/error state, though `Avatar.Fallback` was deliberately skipped (its default mesh-gradient placeholder doesn't fit GymCrew's palette; `Avatar.Root`'s own `backgroundColor` already shows through on a failed image, same as the old `bg-divider`). | **migrated** |
| `AvatarGeneratorModal.tsx`, `AvatarPickerGrid.tsx`, `CrewAvatarGeneratorModal.tsx` | `avatar` + `dialog` | Composition | not started |
| `PhotoCaptureGuide.tsx` | none | Custom build — camera + SVG overlay, not a swappable primitive (see Screen Inventory HIGH RISK note) | not started |
| `ProgressPhotoOverlay.tsx` | none | Custom build — reveal-slider clip logic | not started |

## other (composite/dev-infra)
`ChallengesTab`, `CrewLeagueTab`, `CrewRivalsTab`, `CrewWarTab`, `StatsTab` — largest refactor targets; extract embedded business logic to `lib/` first (see Feature Inventory), then rebuild each tab's shell from the primitives above. `AppTourOverlay` — evaluate whether any Reacticx component covers spotlight-tour behavior (none currently in the catalog); likely keep `react-native-spotlight-tour` underneath. `NutritionCalendar` — bespoke joined-pill-bar rendering not covered by any Reacticx component; keep custom, re-skin chrome. `PlaceholderScreen` — **0 usages found, flag for deletion, not migration.** `StatRow.tsx` — split its 3 bundled exports (`StatSectionHeader`, `StatCard`, `StatRow`) into 3 discrete files during migration for clarity.

---

## Recommended migration order (component layer, Phase 3) — all 8 items resolved 2026-09-16

1. ~~`ConfirmModal` → `primitives/dialog`~~ **migrated** — fixes the documented Alert.alert web bug everywhere it's adopted
2. ~~`ProgressBar` → `organisms/progress`~~ **migrated**
3. ~~`Stepper`, `FormField`~~ **kept custom** — no real Reacticx equivalent exists for either (see "input" section above); both investigated and ruled out with specific reasoning, not skipped
4. ~~`Skeleton`/`HomeSkeleton` → `molecules/Shimmer`~~ **migrated**
5. ~~`AvatarStack` → `primitives/avatar`~~ **migrated** (no dedicated `avatar-group` in the real registry — composed from `avatar` + the existing overlap styling)
6. ~~Investigate `charts/line-chart` vs. `StrengthProgressChart`~~ **investigated, declined** — `line-chart` has no axis-label rendering at all; keeping `react-native-gifted-charts`. See "chart" section above.
7. ~~`BottomSheet`~~ **decided — stays on `@gorhom/bottom-sheet`.** `floating-sheet` turned out to be a different third-party library wrapped in a media-player demo, not a Reacticx primitive. No swap; re-skin only if/when its screen dependents are touched.
8. Everything else, screen-by-screen per the phased plan in `GYMCREW_REACTICX_MIGRATION.md` — **next**, starting Phase 4 (Authentication + Onboarding)

Component-layer investigation is now complete: every open question from Phase 1 discovery has a documented answer, whether that answer was "migrate," "keep custom," or "investigated and declined." Phase 3 closes here; remaining component work (`RankBadge`, `MuscleHeatmap`, `EditableText`, `SkewedStat`, `DivisionBadge`, and everything else in the full catalog above) happens screen-by-screen as each feature area is migrated in Phases 4-11, not as a standalone batch — most of them are branded/bespoke and only need their surrounding chrome touched when their owning screen is actually redesigned.

**Phase 4 update:** `OnboardingFooter`, `SocialAuthButton`, and `VerificationCodeModal` are now migrated too (see their rows above) — Phase 4 (Authentication + Onboarding) started with the components shared across every auth/onboarding screen rather than working screen-by-screen from the top, since those three components alone touch all ~19 screens in this feature area.

**New shared component (not in the original catalog, found during Phase 4): `AuthSubmitButton.tsx`** — `sign-in.tsx`, `sign-up.tsx`, and `forgot-password.tsx` (×2) each had their own byte-for-byte identical hand-rolled full-width submit button (yellow pill, arrow-forward icon, `submitting ? "X-ing..." : "Label"` text swap), and `sign-in.tsx`/`auth-handoff.tsx` (×2) each had an identical compact retry-button variant. Extracted once, built on `base/button` (same `fullWidth` addition as `OnboardingFooter`/`SocialAuthButton`), now used at all 7 call sites. **Migrated and visually verified** (screenshots of all 4 auth screens, zero console errors).

Branded/bespoke visuals (`MuscleHeatmap`, `RankBadge`, `DivisionBadge`, `BadgeRevealFx`, etc.) are never "migrated to Reacticx" in the sense of being replaced — they get their surrounding chrome (padding, shadows, pressable states) rebuilt on Reacticx primitives while the branded asset/animation core stays untouched.
