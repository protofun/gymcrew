# Uploaded files only — never let anything here execute as a script, even if a future bug ever
# let an unexpected filename/extension through.
<FilesMatch "\.(php|phtml|php3|php4|php5|php7|phar|cgi|pl)$">
  Require all denied
</FilesMatch>
